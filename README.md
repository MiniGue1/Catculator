What it does
- Presents a realistic-looking calculator UI using Tkinter.
- When you press `=`, the calculator will now only show the embedded ASCII-art cat (it does not evaluate expressions).

How to run
- Run `python calculator.py` in this folder.

Notes
- All previous files were deleted as you requested.
- The ASCII cat is embedded in the source (no external images).
